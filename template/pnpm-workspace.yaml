# pnpm 11 起，构建脚本白名单等设置从 package.json 的 pnpm 字段迁移到此文件。
# 允许 msw 运行安装脚本（生成/校验 Service Worker），避免 ERR_PNPM_IGNORED_BUILDS
# 阻断 `pnpm exec`（会连带使 git 钩子里的 lint-staged / commitlint 失败）。
allowBuilds:
  msw: true
